# --- Webapp (existant) ---
fastapi==0.78.0
pydantic
uvicorn[standard]

# --- Pentest / Sécurité ---
requests>=2.31.0        # HTTP client (module 03_web)
dnspython>=2.4.0        # Résolution DNS avancée (module 04_recon)
PyJWT>=2.8.0            # Analyse et génération de JWT (module 06_crypto)

# --- Optionnel (confort) ---
# tqdm>=4.66.0          # Barres de progression
# python-whois>=0.9.0   # WHOIS lookup
# scapy>=2.5.0          # Manipulation de paquets réseau (nécessite root)
# impacket>=0.11.0      # Protocoles Windows (SMB, NTLM, Kerberos)
